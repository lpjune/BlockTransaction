extends layout

block content
  h1= title
  
  ul
  - block_list.sort(function(a, b) {let textA = a.title.toUpperCase(); let textB = b.title.toUpperCase(); return (textA < textB) ? -1 : (textA > textB) ? 1 : 0;});
  each block in block_list
    li 
      a(href=block.url) #{block.title} 

  else
    li There are no blocks.
